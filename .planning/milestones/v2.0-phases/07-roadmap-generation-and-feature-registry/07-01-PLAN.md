---
phase: 07-roadmap-generation-and-feature-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/roadmap/types.ts
  - src/roadmap/frontmatter.ts
  - src/roadmap/slug.ts
  - src/roadmap/feature-file.ts
  - src/roadmap/roadmap-file.ts
  - tests/roadmap/frontmatter.test.ts
  - tests/roadmap/slug.test.ts
  - tests/roadmap/feature-file.test.ts
  - tests/roadmap/roadmap-file.test.ts
autonomous: true
requirements: [ROAD-02, ROAD-03, FEAT-01, FEAT-02]

must_haves:
  truths:
    - "Feature frontmatter can be parsed from and stringified to YAML format with all 6 fields"
    - "Slugs generated from feature titles are URL-safe, lowercase, and capped at 50 chars"
    - "Feature files can be read from and written to disk with frontmatter + markdown body"
    - "Roadmap markdown can be generated from milestone and feature data"
    - "Feature status type enforces the unassigned -> assigned -> in-progress -> complete lifecycle"
  artifacts:
    - path: "src/roadmap/types.ts"
      provides: "Feature, FeatureStatus, Milestone, FeatureFrontmatter, RoadmapData types"
      exports: ["Feature", "FeatureStatus", "Milestone", "FeatureFrontmatter", "RoadmapData", "FEATURE_STATUSES"]
    - path: "src/roadmap/frontmatter.ts"
      provides: "YAML frontmatter parse/stringify for feature files"
      exports: ["parseFrontmatter", "stringifyFrontmatter"]
    - path: "src/roadmap/slug.ts"
      provides: "Title-to-slug, feature filename, and branch name generation"
      exports: ["slugify", "featureFilename", "featureBranch"]
    - path: "src/roadmap/feature-file.ts"
      provides: "Read/write individual feature files, read all features from directory"
      exports: ["writeFeatureFile", "readFeatureFile", "readAllFeatures"]
    - path: "src/roadmap/roadmap-file.ts"
      provides: "Generate ROADMAP.md content from structured data"
      exports: ["generateRoadmapMarkdown"]
  key_links:
    - from: "src/roadmap/feature-file.ts"
      to: "src/roadmap/frontmatter.ts"
      via: "import parseFrontmatter, stringifyFrontmatter"
      pattern: "import.*from.*frontmatter"
    - from: "src/roadmap/feature-file.ts"
      to: "src/roadmap/slug.ts"
      via: "import featureFilename"
      pattern: "import.*from.*slug"
    - from: "src/roadmap/roadmap-file.ts"
      to: "src/roadmap/types.ts"
      via: "import Milestone, Feature types"
      pattern: "import.*from.*types"
---

<objective>
Create the pure-function roadmap module (`src/roadmap/`) with types, YAML frontmatter parsing, slug generation, feature file I/O, and roadmap markdown generation.

Purpose: Build all reusable building blocks that Plan 02 (plan-roadmap CLI) and Plan 03 (features CLI) compose into user-facing commands. Separating pure functions from CLI I/O follows the Phase 6 pattern (prfaq/ module before CLI handler).

Output: 5 source files in `src/roadmap/`, 4 test files in `tests/roadmap/`, all tests passing.
</objective>

<execution_context>
@/Users/thumin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thumin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-roadmap-generation-and-feature-registry/07-CONTEXT.md
@.planning/phases/07-roadmap-generation-and-feature-registry/07-RESEARCH.md
@.planning/phases/06-pr-faq-ingestion/06-01-SUMMARY.md

<interfaces>
<!-- Existing constants and utilities the executor will use -->

From src/constants.ts:
```typescript
export const BRANCHOS_DIR = '.branchos';
export const SHARED_DIR = 'shared';
export const STRIP_PREFIXES: string[] = [
  'feature/', 'fix/', 'hotfix/', 'bugfix/', 'release/',
];
```

From src/prfaq/hash.ts (pattern reference for frontmatter parsing):
```typescript
export function splitIntoSections(content: string): Map<string, string>;
export async function readMeta(sharedDir: string): Promise<PrfaqMeta | null>;
export async function writeMeta(sharedDir: string, meta: PrfaqMeta): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create types, frontmatter, and slug modules with tests</name>
  <files>src/roadmap/types.ts, src/roadmap/frontmatter.ts, src/roadmap/slug.ts, tests/roadmap/frontmatter.test.ts, tests/roadmap/slug.test.ts</files>
  <behavior>
    - parseFrontmatter("---\nid: F-001\ntitle: User Auth\nstatus: unassigned\nmilestone: M1\nbranch: feature/user-auth\nissue: null\n---\n\n# Body") returns { data: { id: "F-001", title: "User Auth", status: "unassigned", milestone: "M1", branch: "feature/user-auth", issue: null }, body: "# Body" }
    - parseFrontmatter with issue: 42 returns issue as number 42
    - parseFrontmatter with title containing colon ("Auth: OAuth2") parses correctly (split on first colon only)
    - parseFrontmatter with missing frontmatter delimiters throws Error
    - stringifyFrontmatter round-trips with parseFrontmatter (stringify then parse returns same data)
    - stringifyFrontmatter with issue: null writes "issue: null"
    - slugify("User Authentication") returns "user-authentication"
    - slugify("Auth: OAuth2 Flow!!!") returns "auth-oauth2-flow"
    - slugify with string longer than 50 chars truncates and does not end with hyphen
    - slugify("") returns ""
    - featureFilename("F-001", "User Auth") returns "F-001-user-auth.md"
    - featureBranch("User Auth") returns "feature/user-auth"
    - FEATURE_STATUSES contains exactly ["unassigned", "assigned", "in-progress", "complete"]
  </behavior>
  <action>
    Create `src/roadmap/types.ts` with:
    - `FeatureStatus` type as union: `"unassigned" | "assigned" | "in-progress" | "complete"`
    - `FEATURE_STATUSES` const array of all valid statuses (for runtime validation)
    - `FeatureFrontmatter` interface: `{ id: string; title: string; status: FeatureStatus; milestone: string; branch: string; issue: number | null }`
    - `Feature` interface extending frontmatter: `FeatureFrontmatter & { body: string; filename: string }`
    - `Milestone` interface: `{ id: string; name: string; features: Feature[] }`
    - `RoadmapData` interface: `{ projectName: string; vision: string; milestones: Milestone[] }`

    Create `src/roadmap/frontmatter.ts` with hand-rolled YAML frontmatter parse/stringify (~40 lines). Parse splits on `---` delimiters, splits each line on first `:` only (handles colons in values). Stringify writes fields in fixed order: id, title, status, milestone, branch, issue. Issue field writes "null" for null values, parses "null" string back to null.

    Create `src/roadmap/slug.ts` with slugify (lowercase, replace non-alphanumeric with hyphens, collapse, trim, cap at 50 chars), featureFilename (`${id}-${slugify(title)}.md`), featureBranch (`feature/${slugify(title)}`).

    Write tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>npx vitest run tests/roadmap/frontmatter.test.ts tests/roadmap/slug.test.ts -x</automated>
  </verify>
  <done>All frontmatter and slug tests pass. Types exported. Round-trip parse/stringify works. Slugs are URL-safe and capped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create feature file and roadmap file modules with tests</name>
  <files>src/roadmap/feature-file.ts, src/roadmap/roadmap-file.ts, tests/roadmap/feature-file.test.ts, tests/roadmap/roadmap-file.test.ts</files>
  <behavior>
    - writeFeatureFile(dir, feature) creates file at dir/F-001-slug.md with frontmatter + body
    - readFeatureFile(filepath) returns Feature with parsed frontmatter and body
    - readAllFeatures(dir) returns Feature[] sorted by id, skips non-feature files
    - readAllFeatures on empty/missing directory returns []
    - generateRoadmapMarkdown(data) includes project name header, vision blockquote, milestone/feature counts
    - generateRoadmapMarkdown(data) includes milestone sections with feature tables (columns: #, Feature, Status, Depends On)
    - generateRoadmapMarkdown(data) includes progress tracking per milestone ("0/N features complete")
    - Feature tables show dependencies as "depends on: F-XXX" or "--" if none
  </behavior>
  <action>
    Create `src/roadmap/feature-file.ts`:
    - `writeFeatureFile(dir: string, feature: Feature): Promise<string>` - writes feature to `dir/${feature.filename}`, creates dir with `mkdir({ recursive: true })`, returns written path
    - `readFeatureFile(filepath: string): Promise<Feature>` - reads file, parses frontmatter, extracts filename from path
    - `readAllFeatures(dir: string): Promise<Feature[]>` - reads all `F-*.md` files from dir, returns sorted by id. Returns empty array if dir doesn't exist (graceful).

    Create `src/roadmap/roadmap-file.ts`:
    - `generateRoadmapMarkdown(data: RoadmapData): string` - generates the full ROADMAP.md content matching the example format from RESEARCH.md. Includes: `# Roadmap: {projectName}` header, `> {vision}` blockquote, `**Milestones:** N | **Features:** M` counts, `---` separator, then for each milestone: `## {id}: {name} (0/N features complete)` with a markdown table of features.
    - Feature table columns: `#`, `Feature`, `Status`, `Depends On`. The `Depends On` column shows dependency feature IDs comma-separated or `--` if none. Dependency data comes from an optional `dependsOn` string array on the Feature type (add `dependsOn?: string[]` to the Feature interface in types.ts).

    Write tests FIRST (RED) using temp directories (same pattern as `tests/cli/ingest-prfaq.test.ts`), then implement (GREEN).
  </action>
  <verify>
    <automated>npx vitest run tests/roadmap/feature-file.test.ts tests/roadmap/roadmap-file.test.ts -x</automated>
  </verify>
  <done>Feature files round-trip through write/read. readAllFeatures returns sorted features. generateRoadmapMarkdown produces correct markdown structure with milestones, tables, and progress counts.</done>
</task>

</tasks>

<verification>
npx vitest run tests/roadmap/ -x
</verification>

<success_criteria>
- All `src/roadmap/` modules export their documented functions
- All `tests/roadmap/` tests pass (frontmatter, slug, feature-file, roadmap-file)
- Frontmatter round-trips correctly (stringify -> parse returns same data)
- Feature files can be written to and read from disk
- Roadmap markdown matches the expected format with milestones, tables, and progress
- No new dependencies added (hand-rolled YAML, no gray-matter)
</success_criteria>

<output>
After completion, create `.planning/phases/07-roadmap-generation-and-feature-registry/07-01-SUMMARY.md`
</output>
