---
phase: 06-pr-faq-ingestion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/prfaq/types.ts
  - src/prfaq/validate.ts
  - src/prfaq/hash.ts
  - tests/prfaq/validate.test.ts
  - tests/prfaq/hash.test.ts
autonomous: true
requirements:
  - PRFAQ-02
  - PRFAQ-03

must_haves:
  truths:
    - "Section detection correctly identifies present and missing PR-FAQ sections from markdown content"
    - "Content hashing produces stable SHA-256 hashes with line-ending normalization"
    - "Section diffing reports added, removed, and modified sections between two PR-FAQ versions"
  artifacts:
    - path: "src/prfaq/types.ts"
      provides: "PrfaqMeta interface, SectionDiff interface, EXPECTED_SECTIONS constant, IngestPrfaqResult type"
      exports: ["PrfaqMeta", "SectionDiff", "SectionDefinition", "EXPECTED_SECTIONS", "IngestPrfaqResult", "IngestPrfaqOptions"]
    - path: "src/prfaq/validate.ts"
      provides: "Section detection and validation logic"
      exports: ["detectSections", "isLikelyPrfaq"]
    - path: "src/prfaq/hash.ts"
      provides: "Content hashing and metadata read/write"
      exports: ["hashContent", "readMeta", "writeMeta", "diffSections"]
    - path: "tests/prfaq/validate.test.ts"
      provides: "Unit tests for section detection"
    - path: "tests/prfaq/hash.test.ts"
      provides: "Unit tests for hashing and diffing"
  key_links:
    - from: "src/prfaq/validate.ts"
      to: "src/prfaq/types.ts"
      via: "imports EXPECTED_SECTIONS and types"
      pattern: "import.*from.*types"
    - from: "src/prfaq/hash.ts"
      to: "src/prfaq/types.ts"
      via: "imports PrfaqMeta and SectionDiff"
      pattern: "import.*PrfaqMeta.*from.*types"
---

<objective>
Create the pure-function core for PR-FAQ ingestion: type definitions, section validation, content hashing, and section diffing, all with comprehensive tests.

Purpose: Establish the tested building blocks that the CLI command handler (Plan 02) will compose. These are pure functions with no I/O dependencies beyond file read/write for metadata, making them straightforward to test.

Output: Three source modules (`src/prfaq/types.ts`, `src/prfaq/validate.ts`, `src/prfaq/hash.ts`) and two test files.
</objective>

<execution_context>
@/Users/thumin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thumin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pr-faq-ingestion/06-CONTEXT.md
@.planning/phases/06-pr-faq-ingestion/06-RESEARCH.md

@src/cli/init.ts
@src/constants.ts
@src/output/index.ts

<interfaces>
<!-- Existing exports the executor needs -->

From src/constants.ts:
```typescript
export const BRANCHOS_DIR = '.branchos';
export const SHARED_DIR = 'shared';
```

From src/output/index.ts:
```typescript
export function output(data: unknown, options: { json: boolean }): void;
export function error(message: string, options: { json: boolean }): void;
export function success(message: string, options: { json: boolean }): void;
export function warning(message: string, options: { json: boolean }): void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create types and section validation with tests</name>
  <files>src/prfaq/types.ts, src/prfaq/validate.ts, tests/prfaq/validate.test.ts</files>
  <behavior>
    - detectSections("## Problem\n## Solution\n## Customer FAQ") returns found: ['problem', 'solution', 'customer-faq'], missing: ['headline', 'subheadline', 'quote', 'cta', 'internal-faq']
    - detectSections with all 8 expected sections returns found with 8 items, missing with 0 items
    - detectSections ignores headings inside fenced code blocks (``` markers)
    - detectSections is case-insensitive ("## PROBLEM" matches 'problem')
    - detectSections handles heading variations ("## Customer Problem" matches 'problem' pattern)
    - isLikelyPrfaq returns true when 2+ expected sections found, false when fewer than 2
    - detectSections handles empty string input (returns all missing)
  </behavior>
  <action>
    1. Create `src/prfaq/types.ts` with:
       - `EXPECTED_SECTIONS` array of `{ id: string; patterns: string[] }` objects covering 8 sections: headline, subheadline, problem, solution, quote, cta, customer-faq, internal-faq. Use patterns from RESEARCH.md Pattern 3.
       - `PrfaqMeta` interface: `{ contentHash: string; ingestedAt: string; version: number; sectionsFound: string[]; sectionsMissing: string[]; sourceSize: number }`
       - `SectionDiff` interface: `{ added: string[]; removed: string[]; modified: string[] }`
       - `IngestPrfaqOptions` interface: `{ json: boolean; force: boolean; cwd?: string }`
       - `IngestPrfaqResult` interface: `{ success: boolean; action: 'ingested' | 'updated' | 'unchanged'; sectionsFound: string[]; sectionsMissing: string[]; warnings: string[]; error?: string; diff?: SectionDiff }`

    2. Create `src/prfaq/validate.ts` with:
       - `detectSections(content: string): { found: string[]; missing: string[] }` -- splits content into lines, tracks fenced code block state (toggle on lines starting with ```), filters for heading lines (`#` prefix) outside code blocks, strips `#` and whitespace, matches against EXPECTED_SECTIONS patterns using case-insensitive `.includes()`. Returns found/missing section IDs.
       - `isLikelyPrfaq(content: string): boolean` -- returns true if `detectSections(content).found.length >= 2`

    3. Write tests FIRST in `tests/prfaq/validate.test.ts` covering all behaviors listed above, then implement to make them pass.
  </action>
  <verify>
    <automated>cd /Volumes/BulkStorage/geek/branchos && npx vitest run tests/prfaq/validate.test.ts</automated>
  </verify>
  <done>detectSections correctly identifies all 8 PR-FAQ section types with case-insensitive fuzzy matching, skips headings in code blocks, and isLikelyPrfaq threshold works at 2+ sections. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create content hashing, metadata I/O, and section diffing with tests</name>
  <files>src/prfaq/hash.ts, tests/prfaq/hash.test.ts</files>
  <behavior>
    - hashContent("hello\r\nworld") equals hashContent("hello\nworld") (CRLF normalization)
    - hashContent produces a 64-character hex string (SHA-256)
    - hashContent of same input always produces same output
    - hashContent of different inputs produces different outputs
    - readMeta returns null when file does not exist
    - readMeta returns parsed PrfaqMeta when file exists
    - writeMeta creates valid JSON file with trailing newline
    - diffSections reports added sections (in new but not old)
    - diffSections reports removed sections (in old but not new)
    - diffSections reports modified sections (same heading, different content)
    - diffSections with identical content returns empty arrays
  </behavior>
  <action>
    1. Create `src/prfaq/hash.ts` with:
       - `hashContent(content: string): string` -- normalize CRLF to LF, then `createHash('sha256').update(normalized, 'utf-8').digest('hex')`. Import from `node:crypto`.
       - `readMeta(sharedDir: string): Promise<PrfaqMeta | null>` -- try/catch readFile of `prfaq-meta.json`, parse JSON, return null on any error.
       - `writeMeta(sharedDir: string, meta: PrfaqMeta): Promise<void>` -- writeFile with `JSON.stringify(meta, null, 2) + '\n'`.
       - `splitIntoSections(content: string): Map<string, string>` -- split markdown by heading boundaries, return map of heading-name to content-below-heading. Track code blocks to avoid splitting on headings inside them.
       - `diffSections(oldContent: string, newContent: string): SectionDiff` -- call splitIntoSections on both, compare keys for added/removed, compare values for modified.

    2. Write tests FIRST in `tests/prfaq/hash.test.ts`, then implement. For readMeta/writeMeta tests, use a temp directory (vitest `beforeEach`/`afterEach` with `fs.mkdtemp`/cleanup).
  </action>
  <verify>
    <automated>cd /Volumes/BulkStorage/geek/branchos && npx vitest run tests/prfaq/hash.test.ts</automated>
  </verify>
  <done>hashContent produces stable cross-platform hashes, metadata read/write round-trips correctly, and diffSections accurately reports added/removed/modified sections. All tests pass.</done>
</task>

</tasks>

<verification>
All pure-function tests pass:
```bash
npx vitest run tests/prfaq/
```
</verification>

<success_criteria>
- `src/prfaq/types.ts` exports all interfaces and EXPECTED_SECTIONS constant
- `src/prfaq/validate.ts` detects 8 PR-FAQ sections with fuzzy matching, handles code blocks
- `src/prfaq/hash.ts` hashes content with CRLF normalization, reads/writes metadata, diffs sections
- All tests in `tests/prfaq/` pass
- No new external dependencies added
</success_criteria>

<output>
After completion, create `.planning/phases/06-pr-faq-ingestion/06-01-SUMMARY.md`
</output>
