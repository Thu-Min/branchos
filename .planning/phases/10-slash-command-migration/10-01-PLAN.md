---
phase: 10-slash-command-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/branchos:map-codebase.md
  - commands/branchos:context.md
  - commands/branchos:discuss-phase.md
  - commands/branchos:plan-phase.md
  - commands/branchos:execute-phase.md
  - commands/branchos:ingest-prfaq.md
  - commands/branchos:plan-roadmap.md
  - commands/branchos:features.md
  - commands/branchos:sync-issues.md
  - commands/branchos:refresh-roadmap.md
  - commands/branchos:create-workstream.md
  - commands/branchos:list-workstreams.md
  - commands/branchos:status.md
  - commands/branchos:archive.md
  - src/commands/index.ts
  - src/commands/markdown.d.ts
  - tsup.config.ts
  - tests/commands/index.test.ts
autonomous: true
requirements:
  - MIGR-01
must_haves:
  truths:
    - "14 .md files exist in commands/ directory at repo root"
    - "Each .md file has valid YAML frontmatter with description and allowed-tools"
    - "src/commands/index.ts exports a COMMANDS record with 14 entries"
    - "tsup builds successfully with .md loader"
    - "4 new slash commands exist: create-workstream, list-workstreams, status, archive"
  artifacts:
    - path: "commands/branchos:map-codebase.md"
      provides: "Extracted map-codebase slash command"
    - path: "commands/branchos:create-workstream.md"
      provides: "New create-workstream slash command"
    - path: "commands/branchos:list-workstreams.md"
      provides: "New list-workstreams slash command"
    - path: "commands/branchos:status.md"
      provides: "New consolidated status dashboard slash command"
    - path: "commands/branchos:archive.md"
      provides: "New archive slash command"
    - path: "src/commands/index.ts"
      provides: "Barrel export of all 14 bundled command strings"
      exports: ["COMMANDS"]
    - path: "src/commands/markdown.d.ts"
      provides: "TypeScript declaration for .md imports"
    - path: "tsup.config.ts"
      provides: "Build config with .md text loader"
      contains: "loader"
  key_links:
    - from: "src/commands/index.ts"
      to: "commands/*.md"
      via: "import default from relative path"
      pattern: "import .* from '../../commands/"
    - from: "tsup.config.ts"
      to: "esbuild loader"
      via: "loader option"
      pattern: "'\\.md': 'text'"
---

<objective>
Extract all 10 existing slash commands from string literals in install-commands.ts to individual .md files in commands/ directory. Create 4 new slash command .md files (create-workstream, list-workstreams, status, archive). Set up tsup .md bundling and the src/commands/index.ts barrel export.

Purpose: Establishes the new .md-based slash command architecture that Plan 02 will wire into the CLI.
Output: 14 .md files in commands/, barrel export in src/commands/, updated tsup config, passing tests.
</objective>

<execution_context>
@/Users/thumin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thumin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-slash-command-migration/10-CONTEXT.md
@.planning/phases/10-slash-command-migration/10-RESEARCH.md

<interfaces>
<!-- Current install-commands.ts COMMANDS record has 10 entries as string literals.
     Keys are: branchos:map-codebase.md, branchos:context.md, branchos:discuss-phase.md,
     branchos:plan-phase.md, branchos:execute-phase.md, branchos:ingest-prfaq.md,
     branchos:plan-roadmap.md, branchos:features.md, branchos:sync-issues.md,
     branchos:refresh-roadmap.md -->

From src/cli/install-commands.ts (line 6):
```typescript
const COMMANDS: Record<string, string> = {
  'branchos:map-codebase.md': `...`,
  'branchos:context.md': `...`,
  // ... 10 total entries
};
```

From tsup.config.ts:
```typescript
export default defineConfig({
  entry: ['src/index.ts'],
  format: ['cjs'],
  target: 'node20',
  clean: true,
  sourcemap: true,
  banner: { js: '#!/usr/bin/env node' },
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract existing slash commands to .md files and create 4 new commands</name>
  <files>commands/branchos:map-codebase.md, commands/branchos:context.md, commands/branchos:discuss-phase.md, commands/branchos:plan-phase.md, commands/branchos:execute-phase.md, commands/branchos:ingest-prfaq.md, commands/branchos:plan-roadmap.md, commands/branchos:features.md, commands/branchos:sync-issues.md, commands/branchos:refresh-roadmap.md, commands/branchos:create-workstream.md, commands/branchos:list-workstreams.md, commands/branchos:status.md, commands/branchos:archive.md</files>
  <behavior>
    - Test: 14 .md files exist in commands/ directory
    - Test: Each file starts with YAML frontmatter (--- delimited) containing "description:" and "allowed-tools:"
    - Test: Each file contains "$ARGUMENTS" placeholder
    - Test: create-workstream.md references "npx branchos workstream create"
    - Test: list-workstreams.md references "npx branchos workstream list"
    - Test: status.md references map-status, check-drift, and detect-conflicts
    - Test: archive.md references "npx branchos archive"
  </behavior>
  <action>
    Create `commands/` directory at repo root. Extract the 10 existing slash command string literals from `src/cli/install-commands.ts` (lines 7-733) into individual .md files. Remove the escaped backticks (replace \` with `) and escaped dollar signs as needed -- the content should be clean markdown, not escaped TypeScript strings.

    Create 4 new .md files following the research patterns:

    **commands/branchos:create-workstream.md**: Delegates to `npx branchos workstream create $ARGUMENTS`. Frontmatter: description "Create a new BranchOS workstream", allowed-tools `Bash(npx branchos *)`. Document --name, --feature, --json flags.

    **commands/branchos:list-workstreams.md**: Delegates to `npx branchos workstream list $ARGUMENTS`. Frontmatter: description "List all BranchOS workstreams with status", allowed-tools `Bash(npx branchos *)`. Document --json flag.

    **commands/branchos:status.md**: Consolidated dashboard calling 4 commands: `npx branchos status --json`, `npx branchos map-status --json`, `npx branchos check-drift --json`, `npx branchos detect-conflicts --all --json`. Frontmatter: description "Show BranchOS dashboard with workstream status, map staleness, drift, and conflicts", allowed-tools `Bash(npx branchos *)`. Instruct Claude to present results as unified dashboard sections.

    **commands/branchos:archive.md**: Delegates to `npx branchos archive $ARGUMENTS`. Frontmatter: description "Archive a completed workstream after branch merge", allowed-tools `Bash(npx branchos *)`. Document --json flag.

    Write tests FIRST in `tests/commands/index.test.ts` using fs to read the .md files directly from `commands/` directory and validate structure. Then create the files to make tests pass.
  </action>
  <verify>
    <automated>npx vitest run tests/commands/index.test.ts</automated>
  </verify>
  <done>14 .md files exist in commands/ with valid frontmatter. 10 existing commands extracted verbatim (unescaped). 4 new commands follow established patterns. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Set up tsup .md loader, TypeScript declaration, and barrel export</name>
  <files>tsup.config.ts, src/commands/markdown.d.ts, src/commands/index.ts</files>
  <action>
    1. Update `tsup.config.ts` to add the .md text loader:
    ```typescript
    loader: {
      '.md': 'text',
    },
    ```
    Add this field to the existing defineConfig object. Keep all other fields unchanged.

    2. Create `src/commands/markdown.d.ts`:
    ```typescript
    declare module '*.md' {
      const content: string;
      export default content;
    }
    ```

    3. Create `src/commands/index.ts` with explicit imports for all 14 .md files from `../../commands/` and export a single `COMMANDS: Record<string, string>` object. Keys are the filenames (e.g., `'branchos:map-codebase.md'`), values are the imported string content. Follow the exact pattern from the research:
    ```typescript
    import mapCodebase from '../../commands/branchos:map-codebase.md';
    // ... all 14 imports
    export const COMMANDS: Record<string, string> = {
      'branchos:map-codebase.md': mapCodebase,
      // ... all 14 entries
    };
    ```

    4. Verify the build works by running `npx tsup`. The build must succeed with no errors.
  </action>
  <verify>
    <automated>npx tsup && echo "Build succeeded"</automated>
  </verify>
  <done>tsup builds successfully with .md files bundled as text strings. src/commands/index.ts exports COMMANDS record with 14 entries. TypeScript declaration prevents TS2307 errors on .md imports.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/commands/index.test.ts` -- all command file structure tests pass
- `npx tsup` -- build succeeds with .md loader
- `ls commands/*.md | wc -l` -- returns 14
</verification>

<success_criteria>
- 14 .md files in commands/ directory with valid YAML frontmatter
- src/commands/index.ts exports COMMANDS record with 14 entries
- tsup builds successfully with .md text loader
- All new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-slash-command-migration/10-01-SUMMARY.md`
</output>
